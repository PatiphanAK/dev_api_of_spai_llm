services:
  app:
    build: .
    container_name: python_app
    environment:
      - MODEL_DIR=/models
      - BUILD_VERSION=xxxxxxx
      - HF_REPO=xxxxxxx
    volumes:
      - ./main.py:/app/main.py
      - ./models:/models
    networks:
      - spaiss6
    ports:
      - "8000:8000"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]

networks:
  spaiss6:
    driver: bridge